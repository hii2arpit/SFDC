({
    doInit : function(component, event, helper) {
        helper.Init(component,event,helper);
    },
    createOpportunity: function(component, event, helper)
    {
        helper.opportunityRecord(component,event,helper);
    }
})
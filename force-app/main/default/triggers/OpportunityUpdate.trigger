trigger OpportunityUpdate on opportunity (before update) {

    // if(trigger.isUpdate && trigger.isbefore)
    // {
    //     updateAccountChildRecords.updateOpportunity(Trigger.new,Trigger.oldMap);
    // }
}